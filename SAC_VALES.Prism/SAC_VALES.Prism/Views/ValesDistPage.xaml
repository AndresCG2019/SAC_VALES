<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             xmlns:busyindicator="clr-namespace:Syncfusion.SfBusyIndicator.XForms;assembly=Syncfusion.SfBusyIndicator.XForms"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="SAC_VALES.Prism.Views.ValesDistPage"
             Title ="{Binding Title}">

    <AbsoluteLayout>
        <StackLayout>
            <CollectionView ItemsSource="{Binding Vales}" Margin="5, 10, 5, 5" EmptyView="Aun no se han registrado vales.">
                <CollectionView.ItemsLayout>
                    <GridItemsLayout Orientation="Vertical"/>
                </CollectionView.ItemsLayout>
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Frame
                        Padding="5"
                        CornerRadius="20"
                        IsClippedToBounds="False"
                        BorderColor="Black">
                            <Grid HeightRequest="75">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="0.2*"  />
                                    <ColumnDefinition Width="0.2*" />
                                    <ColumnDefinition Width="0.5*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="20" />
                                    <RowDefinition Height="20" />
                                    <RowDefinition Height="40" />
                                </Grid.RowDefinitions>
                                
                                <Label Text="FOLIO" FontAttributes="Bold" />
                                <Label Grid.Column="1" Text="MONTO" FontAttributes="Bold" />
                                <Label Grid.Column="2" Text="CLIENTE" FontAttributes="Bold" />
                                
                                <Label Grid.Row="1" Text="{Binding NumeroFolio}" />
                                <Label Grid.Column="1" Grid.Row="1" Text="{Binding Monto} " />
                                <Label Grid.Column="2" Grid.Row="1"  Text="{Binding Cliente.Email}" />
                                
                            </Grid>
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </StackLayout>
        <busyindicator:SfBusyIndicator AnimationType="Ball" 
                                       AbsoluteLayout.LayoutBounds=".5,.5,.5,.5"
                	                   AbsoluteLayout.LayoutFlags="All"
                	                   BackgroundColor="{StaticResource ColorPrimary}"
                	                   HorizontalOptions="Center"
                	                   TextColor="{StaticResource ColorFontInverse}"
                	                   IsBusy="{Binding IsRunning}"
                	                   Title="cargando"
                	                   VerticalOptions="Center"
                	                   ViewBoxWidth="80"
                	                   ViewBoxHeight="80" />
    </AbsoluteLayout>
  
</ContentPage>